{
  "name": "sorting-algorithms-visualization",
  "version": "1.0.0",
  "scripts": {
    "watch": "esbuild src/main.ts --bundle --watch --outfile=build/main.js",
    "build": "esbuild src/main.ts --bundle --minify --outfile=build/main.js",
    "start": "esbuild --serve --servedir=."
  },
  "keywords": [
    "sorting"
  ],
  "author": "Alex Lima",
  "license": "MIT",
  "description": "visualization of sorting algorithms using html canvas",
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@typescript-eslint/eslint-plugin": "^8.65.0",
    "@typescript-eslint/parser": "^8.65.0",
    "esbuild": "^0.23.0",
    "eslint": "^9.39.5",
    "typescript-eslint": "^8.65.0"
  }
}
